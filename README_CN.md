<div style="text-align:center">

<h1>AI 领域新闻整合器</h1>

<b>中文说明 | <a href = "./README.md">English</a></b> <br>
<b>Version: v1.0.0</b>

</div>

**AI 领域新闻整合器 (AI Domain News Integrator)** 是一款基于大模型的智能信息聚合工具。它可以根据您设定的**特定领域或话题**，自动构建检索策略，从全网抓取、分析并生成深度日报。

不同于简单的关键词搜索，本项目利用 AI **动态发现**高质量垂直信源，并整合全网信息，为您提供结构化、有洞见的领域情报。

### ✨ 核心特性 (Key Features)

*   **🧠 AI 动态规划 (AI-Driven Source Discovery)**: 告别死板的静态配置。AI 代理根据话题自动“猜测”并规划最相关的垂直领域信源（如 Hupu, GitHub, WSJ），构建专属的信息获取网络。
*   **🛡️ 高鲁棒性架构 (High Robustness)**: 
    *   **多重重试**: 应对网络抖动与接口限流。
    *   **补偿机制**: 自动触发回填搜索（Gap Fill），确保信息量充足。
    *   **分层采样**: 保证核心信源权重的同时，不遗漏长尾信息。
*   **⚡ 高并发设计 (Concurrent & Efficient)**: 优化的多线程并发抓取引擎，极速完成多信源、大规模的信息采集任务。
*   **📊 全流程智能整合 (Smart Integration)**: 集多源聚合、深度摘要与结构化输出于一体。利用大模型（Qwen-Max等）过滤噪音，自动生成含“摘要+点评”的现代化 **HTML 日报**。


### 1. 基础环境准备
请确保您的电脑已安装 **Python 3.10+**。
检查命令：
- **macOS / Linux**: `python3 --version`
- **Windows**: `python --version`

### 2. 克隆项目与安装依赖
打开终端，执行以下命令：

**下载代码**
```bash
git clone git@github.com:jgmiao/ai-news.git ai-news-integrator
cd ai-news-integrator
```

**创建虚拟环境 (推荐)**
```bash
# macOS / Linux
python3 -m venv .venv
source .venv/bin/activate

# Windows
python -m venv .venv
.venv\Scripts\activate
```

**安装依赖**
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## ⚙️ 快速配置

### 1. 准备大模型 API
本项目兼容所有 OpenAI 格式的接口。

**配置参数:**
*   `api_key`: 鉴权密钥 (支持环境变量 `API_KEY`)。
*   `base_url`: 模型服务地址 (支持环境变量 `BASE_URL`)。
*   `model_name`: 模型名称 (支持环境变量 `MODEL_NAME`)。

**获取 api_key**
*   **阿里云 Qwen**: 登录 [阿里云百炼平台](https://bailian.console.aliyun.com/) -> "API-KEY" -> "创建新的API-KEY"。
*   **DeepSeek / OpenAI**: 访问各自官网控制台获取。

**获取 base_url（以官网更新为准）**
*   **阿里云 Qwen**: `https://dashscope.aliyuncs.com/compatible-mode/v1`
*   **DeepSeek**: `https://api.deepseek.com`
*   **OpenAI**: `https://api.openai.com/v1`

**获取model_name（以官网更新为准）**
*   **Qwen (Alibaba)**: `qwen-max` (Best), `qwen-plus`.
*   **DeepSeek**: `deepseek-chat`.
*   **OpenAI**: `gpt-4o`.
*   **Google Gemini**: `gemini-pro`.

### 2. 编辑配置文件
复制 `config.yaml` 或直接编辑：

**推荐配置 (`config.yaml`)：**

```yaml
llm:
  # 阿里千问配置示例:
  api_key: "sk-xxxxxxxx"  # 您的 API Key
  base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
  model_name: "qwen-max" 
``` 

```yaml
search:
  # 总新闻目标数
  total_news: 50 
  min_per_core_source: 3
  
  # 网络代理配置 (可选)
  # 1. 访问外网 (如 Google/GitHub): 必须配置。请填写您的本地代理地址。
  # 2. 无代理 / 仅国内使用: 请将以下两行注释掉，或设为 null。
  proxy:
    http: "http://127.0.0.1:7890"  
    https: "http://127.0.0.1:7890"

  news_sources:
    core:
      items:
        google_news: { match_names: ["google_news"] }
        github: { match_names: ["GitHub"], search_query: "site:github.com" }
        # ... (更多源请参考 config.yaml 默认配置)
```

### 3. 环境变量 (优先级最高)
环境变量会覆盖配置文件中的对应设置。

- `DASHSCOPE_API_KEY`
- `DASHSCOPE_BASE_URL`
- `MODEL_NAME`
- `NEWS_TOPIC` (可选，用于自动化运行)

## 使用方法

### 交互模式
直接运行脚本，按提示输入关键词即可。

```bash
./run.sh
# 输入: "AI Agent"

```

### 命令行参数
也可以直接带参数运行。

```bash
./run.sh "AI Agent"

```

### 自动化运行 (环境变量)
通过设置 `NEWS_TOPIC` 变量实现无交互运行。

```bash
export NEWS_TOPIC="生成式 AI"
./run.sh

```

## 输出示例

脚本会在 `./docs` 目录（或您配置的路径）下生成 HTML 报告。

**文件示例 (`./docs/AI_Agent_2026-01-18.html`):**

报告采用现代化的 HTML5 卡片式布局，内嵌 CSS 样式，无需任何插件即可在浏览器中获得最佳阅读体验。



---

## 许可证

[MIT](LICENSE)